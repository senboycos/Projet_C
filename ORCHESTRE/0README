Le fichier "orchestre.c" contient le code de l'orchestre .

L'orchestre se lance à partir du répertoire racine du projet (i.e. src).

Les fichiers "client_orchestre.h" et "client_orchestre.c" contiennent
les routines de communication entre l'orchestre et les clients (tubes
nommés et sémaphore). Les fichiers ne sont pas dans ce répertoire, mais dans
le répertoire de CLIENT_ORCHESTRE.
Note importante : il faut mettre une part importante du code dans ces fichiers
    de façon à avoir un main le plus court et le plus lisible possible

Les fichiers "service_orchestre.h" et "service_orchestre.c" contiennent
les routines de communication entre l'orchestre et les services (tubes
anonymes et sémaphores). Les fichiers ne sont pas dans ce répertoire, mais dans
le répertoire de ORCHESTRE_SERVICE.
Note importante : cf. ci-dessus

